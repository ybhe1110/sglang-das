name: Nightly Test (HCU)

on:
  schedule:
    - cron: "30 18 * * *" # 02:30 Beijing Time daily
  workflow_dispatch:
    inputs:
      ref:
        description: "Git ref, branch, tag, or SHA to test"
        required: false
        type: string
        default: ""
      runner_label:
        description: "HCU runner label override"
        required: false
        type: string
        default: ""
      image:
        description: "HCU CI container image override"
        required: false
        type: string
        default: ""
      continue_on_error:
        description: "Continue running remaining files after a test failure"
        required: false
        type: boolean
        default: true
      job_filter:
        description: "Run one HCU nightly module or all modules"
        required: false
        type: choice
        default: "all"
        options:
          - all
          - nightly-hcu-core
          - nightly-hcu-core-functional
          - nightly-hcu-1
          - nightly-hcu-accuracy
          - nightly-hcu-accuracy-text
          - nightly-hcu-accuracy-reasoning-code
          - nightly-hcu-vlm
          - nightly-hcu-perf-text
          - nightly-hcu-large-model-4
          - nightly-hcu-large-model-8
          - nightly-hcu-quant-opt
          - nightly-hcu-functional-long
          - nightly-hcu-api-models
          - nightly-hcu-long-scheduler
          - nightly-hcu-long-decoding
          - nightly-hcu-2
          - nightly-hcu-4-functional

permissions:
  contents: read

concurrency:
  group: nightly-test-hcu-${{ github.event_name == 'workflow_dispatch' && format('manual-{0}', github.run_id) || github.ref }}
  cancel-in-progress: ${{ github.event_name != 'workflow_dispatch' }}

jobs:
  validate-config:
    name: Validate HCU nightly config
    runs-on: ${{ inputs.runner_label || vars.HCU_CI_RUNNER_LABEL }}
    outputs:
      runner_label: ${{ steps.resolve.outputs.runner_label }}
      image: ${{ steps.resolve.outputs.image }}
      continue_on_error_flag: ${{ steps.resolve.outputs.continue_on_error_flag }}
      job_filter: ${{ steps.resolve.outputs.job_filter }}
      target_ref: ${{ steps.resolve.outputs.target_ref }}
    steps:
      - name: Resolve HCU runner, image, ref, and filters
        id: resolve
        env:
          INPUT_RUNNER_LABEL: ${{ inputs.runner_label || '' }}
          INPUT_IMAGE: ${{ inputs.image || '' }}
          INPUT_REF: ${{ inputs.ref || '' }}
          INPUT_JOB_FILTER: ${{ inputs.job_filter || 'all' }}
          VAR_RUNNER_LABEL: ${{ vars.HCU_CI_RUNNER_LABEL || '' }}
          VAR_IMAGE: ${{ vars.HCU_CI_IMAGE_0518 || '' }}
          CONTINUE_ON_ERROR: ${{ inputs.continue_on_error || '' }}
          GITHUB_SHA_VALUE: ${{ github.sha }}
        run: |
          set -euo pipefail
          runner_label="${INPUT_RUNNER_LABEL:-${VAR_RUNNER_LABEL}}"
          if [[ "${runner_label}" == "hcu-ci-pr" ]]; then
            echo "Routing validation runner label hcu-ci-pr to hcu-ci-pr-2 for nmz4 test runner."
            runner_label="hcu-ci-pr-2"
          fi
          image="${INPUT_IMAGE:-${VAR_IMAGE}}"
          target_ref="${INPUT_REF:-${GITHUB_SHA_VALUE}}"
          continue_on_error="${CONTINUE_ON_ERROR:-true}"
          job_filter="${INPUT_JOB_FILTER:-all}"

          case "${job_filter}" in
            nightly-hcu-core)
              job_filter="nightly-hcu-core-functional"
              ;;
            nightly-hcu-accuracy)
              job_filter="nightly-hcu-accuracy-text"
              ;;
            nightly-hcu-long-scheduler|nightly-hcu-long-decoding)
              job_filter="nightly-hcu-functional-long"
              ;;
          esac

          if [[ -z "${runner_label}" ]]; then
            echo "::error::HCU runner label is required. Set vars.HCU_CI_RUNNER_LABEL or workflow input runner_label."
            exit 1
          fi

          if [[ -z "${image}" ]]; then
            echo "::error::HCU CI image is required. Set vars.HCU_CI_IMAGE_0518 or workflow input image."
            exit 1
          fi

          continue_on_error_flag=""
          if [[ "${continue_on_error}" == "true" || "${GITHUB_EVENT_NAME}" == "schedule" ]]; then
            continue_on_error_flag="--continue-on-error"
          fi

          echo "runner_label=${runner_label}" >> "${GITHUB_OUTPUT}"
          echo "image=${image}" >> "${GITHUB_OUTPUT}"
          echo "target_ref=${target_ref}" >> "${GITHUB_OUTPUT}"
          echo "continue_on_error_flag=${continue_on_error_flag}" >> "${GITHUB_OUTPUT}"
          echo "job_filter=${job_filter}" >> "${GITHUB_OUTPUT}"

      - name: Checkout code
        env:
          CHECKOUT_REF: ${{ steps.resolve.outputs.target_ref }}
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          CHECKOUT_DIR="${RUNNER_TEMP}/hcu-sglang-${GITHUB_RUN_ID}-nightly-validate"
          rm -rf "${CHECKOUT_DIR}"
          mkdir -p "${CHECKOUT_DIR}"
          echo "HCU_CI_CHECKOUT_DIR=${CHECKOUT_DIR}" >> "${GITHUB_ENV}"
          git init "${CHECKOUT_DIR}"
          cd "${CHECKOUT_DIR}"
          git remote add origin "https://github.com/${GITHUB_REPOSITORY}.git"
          auth="$(printf 'x-access-token:%s' "${GH_TOKEN}" | base64 | tr -d '\n')"
          git \
            -c http.https://github.com/.extraheader="AUTHORIZATION: basic ${auth}" \
            -c http.https://github.com.proxy= \
            -c http.proxy= \
            -c https.proxy= \
            fetch --no-tags --depth=1 origin "${CHECKOUT_REF}"
          git checkout --force FETCH_HEAD

      - name: Setup HCU validation container
        env:
          HCU_CI_IMAGE: ${{ steps.resolve.outputs.image }}
          HCU_CI_CONTAINER_NAME: ci_sglang_hcu_nightly_${{ github.run_id }}_validate
          HCU_CI_SKIP_PULL: ${{ vars.HCU_CI_SKIP_PULL || '' }}
          HCU_CI_VISIBLE_DEVICES: ${{ vars.HCU_CI_VISIBLE_DEVICES || '' }}
        run: |
          GITHUB_WORKSPACE="${HCU_CI_CHECKOUT_DIR}" \
            bash "${HCU_CI_CHECKOUT_DIR}/scripts/ci/hcu/hcu_ci_start_container.sh" \
              --image "${HCU_CI_IMAGE}" \
              --container-name "${HCU_CI_CONTAINER_NAME}"

      - name: Verify HCU registration in container
        env:
          HCU_CI_CONTAINER_NAME: ci_sglang_hcu_nightly_${{ github.run_id }}_validate
        run: |
          bash "${HCU_CI_CHECKOUT_DIR}/scripts/ci/hcu/hcu_ci_exec.sh" \
            --container-name "${HCU_CI_CONTAINER_NAME}" \
            -w /sglang-checkout \
            -- python3 scripts/ci/hcu/verify_hcu_registration.py

      - name: Cleanup HCU validation container
        if: always()
        env:
          HCU_CI_CONTAINER_NAME: ci_sglang_hcu_nightly_${{ github.run_id }}_validate
        run: bash "${HCU_CI_CHECKOUT_DIR}/scripts/ci/hcu/hcu_ci_cleanup_container.sh"

  probe-hcu-wheels:
    name: Probe staged HCU wheels
    needs: validate-config
    if: needs.validate-config.result == 'success'
    runs-on: ${{ needs.validate-config.outputs.runner_label }}
    outputs:
      full_sha: ${{ steps.probe.outputs.full_sha }}
      short_sha: ${{ steps.probe.outputs.short_sha }}
      wheel_available: ${{ steps.probe.outputs.wheel_available }}
      wheel_urls: ${{ steps.probe.outputs.wheel_urls }}
    env:
      CHECKOUT_REF: ${{ needs.validate-config.outputs.target_ref }}
      HCU_WHEEL_STAGING_ROOT: /ci_public/sglang-das/hcu-wheel-staging
      HCU_WHEEL_STAGING_CONTAINER_ROOT: /hcu-wheel-staging
    steps:
      - name: Checkout code
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          CHECKOUT_DIR="${RUNNER_TEMP}/hcu-sglang-${GITHUB_RUN_ID}-nightly-probe-wheels"
          rm -rf "${CHECKOUT_DIR}"
          mkdir -p "${CHECKOUT_DIR}"
          echo "HCU_CI_CHECKOUT_DIR=${CHECKOUT_DIR}" >> "${GITHUB_ENV}"
          git init "${CHECKOUT_DIR}"
          cd "${CHECKOUT_DIR}"
          git remote add origin "https://github.com/${GITHUB_REPOSITORY}.git"
          auth="$(printf 'x-access-token:%s' "${GH_TOKEN}" | base64 | tr -d '\n')"
          git \
            -c http.https://github.com/.extraheader="AUTHORIZATION: basic ${auth}" \
            -c http.https://github.com.proxy= \
            -c http.proxy= \
            -c https.proxy= \
            fetch --no-tags --depth=1 origin "${CHECKOUT_REF}"
          git checkout --force FETCH_HEAD

      - name: Probe local staged wheels
        id: probe
        run: |
          set -euo pipefail
          full_sha="$(git -C "${HCU_CI_CHECKOUT_DIR}" rev-parse HEAD)"
          short_sha="$(git -C "${HCU_CI_CHECKOUT_DIR}" rev-parse --short=12 HEAD)"
          export HCU_WHEEL_TARGET_SHA="${full_sha}"
          export HCU_WHEEL_TARGET_SHORT_SHA="${short_sha}"
          echo "full_sha=${full_sha}" >> "${GITHUB_OUTPUT}"
          echo "short_sha=${short_sha}" >> "${GITHUB_OUTPUT}"

          python3 - <<'PY_PROBE' | tee /tmp/hcu_nightly_probe_wheels.out
          import os
          import pathlib
          import sys

          root = pathlib.Path(os.environ.get("HCU_WHEEL_STAGING_ROOT", "/ci_public/sglang-das/hcu-wheel-staging"))
          container_root = pathlib.Path(os.environ.get("HCU_WHEEL_STAGING_CONTAINER_ROOT", "/hcu-wheel-staging"))
          target_sha = os.environ["HCU_WHEEL_TARGET_SHA"]
          repo = os.environ["GITHUB_REPOSITORY"]
          owner, repo_name = repo.split("/", 1)
          repo_key = f"{owner}_{repo_name}"
          def candidate_dirs():
              base = root / repo_key
              return sorted(
                  [path for path in base.glob(f"pr-*/{target_sha}") if path.is_dir()],
                  key=lambda path: path.stat().st_mtime,
                  reverse=True,
              )

          def wheel_kind(path: pathlib.Path):
              name = path.name.replace("-", "_")
              if name.startswith(("sgl_kernel_", "sglang_kernel_")):
                  return "sgl-kernel"
              if name.startswith("sglang_"):
                  return "sglang"
              return None

          def to_container_path(path: pathlib.Path) -> str:
              return str(container_root / path.relative_to(root))

          candidates = candidate_dirs()
          if not candidates:
              print("wheel_available=false")
              print(f"[probe-hcu-wheels] no staged dirs under {root / repo_key}/pr-*/{target_sha}", file=sys.stderr)
              sys.exit(0)

          for staging_dir in candidates:
              ready = staging_dir / "READY"
              wheel_dir = staging_dir / "wheels"
              if not ready.exists() or not wheel_dir.is_dir():
                  print(f"[probe-hcu-wheels] skipping incomplete staging dir {staging_dir}", file=sys.stderr)
                  continue
              found = {}
              for wheel in sorted(wheel_dir.glob("*.whl")):
                  kind = wheel_kind(wheel)
                  if kind and kind not in found:
                      found[kind] = wheel
              missing = [name for name in ("sglang", "sgl-kernel") if name not in found]
              if not missing:
                  wheel_urls = " ".join(to_container_path(found[name]) for name in ("sglang", "sgl-kernel"))
                  print("wheel_available=true")
                  print(f"wheel_urls={wheel_urls}")
                  print(f"[probe-hcu-wheels] reusing staged wheels from {staging_dir}: {wheel_urls}", file=sys.stderr)
                  sys.exit(0)
              print(f"[probe-hcu-wheels] {staging_dir} missing {missing}; found={list(found)}", file=sys.stderr)

          print("wheel_available=false")
          PY_PROBE

          wheel_available="$(grep '^wheel_available=' /tmp/hcu_nightly_probe_wheels.out | tail -n 1 | cut -d= -f2-)"
          wheel_urls="$(grep '^wheel_urls=' /tmp/hcu_nightly_probe_wheels.out | tail -n 1 | cut -d= -f2- || true)"
          echo "wheel_available=${wheel_available:-false}" >> "${GITHUB_OUTPUT}"
          echo "wheel_urls=${wheel_urls}" >> "${GITHUB_OUTPUT}"

  build-hcu-wheels:
    name: Build HCU nightly wheels
    needs:
      - validate-config
      - probe-hcu-wheels
    if: needs.validate-config.result == 'success' && needs.probe-hcu-wheels.outputs.wheel_available != 'true'
    runs-on: [self-hosted, hcu, nmz4]
    timeout-minutes: 180
    defaults:
      run:
        shell: bash
    container:
      image: ${{ vars.HCU_CI_RELEASE_IMAGE }}
      options: >
        --device=/dev/kfd --device=/dev/dri/ --group-add video
        -v /opt/hyhal/:/opt/hyhal/:ro
        --shm-size=80G
        --cap-add=SYS_PTRACE
        --security-opt seccomp=unconfined
        --privileged=true
        -v /ci_public/sglang-das/hcu-wheel-staging:/ci_public/sglang-das/hcu-wheel-staging
    env:
      TORCH_VERSION: '2.10.0'
      PIP_INDEX_URL: http://10.16.1.201:9929/nightly/dtk2604/+simple/
      PIP_TRUSTED_HOST: 10.16.1.201
      RESOURCE_SERVER: http://10.16.1.201:8000/
      WHEEL_COMMIT_SHA: ${{ needs.probe-hcu-wheels.outputs.full_sha }}
      HCU_WHEEL_STAGING_ROOT: /ci_public/sglang-das/hcu-wheel-staging
    steps:
      - name: Detect runner workspace ownership
        shell: bash
        run: |
          host_uid="$(stat -c '%u' "${RUNNER_TEMP}")"
          host_gid="$(stat -c '%g' "${RUNNER_TEMP}")"
          [[ "${host_uid}" =~ ^[0-9]+$ && "${host_gid}" =~ ^[0-9]+$ ]]
          echo "HCU_RUNNER_HOST_UID=${host_uid}" >> "${GITHUB_ENV}"
          echo "HCU_RUNNER_HOST_GID=${host_gid}" >> "${GITHUB_ENV}"

      - name: Normalize workspace ownership before checkout
        run: |
          chown -R "${HCU_RUNNER_HOST_UID}:${HCU_RUNNER_HOST_GID}" "${GITHUB_WORKSPACE}"

      - uses: actions/checkout@v4
        with:
          ref: ${{ env.WHEEL_COMMIT_SHA }}
          submodules: 'recursive'

      - name: Trust checked-out workspace
        run: |
          git config --global --add safe.directory "${GITHUB_WORKSPACE}"

      - name: Install build deps
        run: |
          pip install torch==${TORCH_VERSION} build scikit-build-core cmake ninja wheel setuptools setuptools-scm setuptools-rust maturin ciupload
          wget ${RESOURCE_SERVER}/Jenkins/CompileDep/sglang/protoc-24.4-linux-x86_64.zip
          unzip protoc-24.4-linux-x86_64.zip -d /usr/local/
          chmod +x /usr/local/bin/protoc
          ln -s /usr/local/bin/protoc /usr/bin/protoc
          wget ${RESOURCE_SERVER}/Jenkins/CompileDep/sglang/rustup-init.sh
          export RUSTUP_DIST_SERVER=https://mirrors.tuna.tsinghua.edu.cn/rustup
          export RUSTUP_UPDATE_ROOT=https://mirrors.tuna.tsinghua.edu.cn/rustup/rustup
          bash rustup-init.sh -y
          rm -rf ${HOME}/.cargo/config
          mkdir -p ${HOME}/.cargo
          cat > ${HOME}/.cargo/config << 'EOF'
          [source.crates-io]
          replace-with = 'rsproxy-sparse'

          [source.tuna-sparse]
          registry = "sparse+https://mirrors.tuna.tsinghua.edu.cn/crates.io-index/"

          [source.rsproxy-sparse]
          registry = "sparse+https://rsproxy.cn/index/"

          [net]
          retry = 5
          git-fetch-with-cli = true

          [http]
          low-speed-limit = 1
          EOF

      - name: Install DTK
        run: |
          DTK_PKG_URL=${RESOURCE_SERVER}/dtk-pkg/dtk26.04/DTK-26.04-rc4-centos8-x86_64.tar.gz
          cd /opt
          wget -q $DTK_PKG_URL
          tar -xzf $(basename $DTK_PKG_URL)
          mv dtk-* dtk

      - name: Build sgl-kernel (HIP)
        run: |
          source /opt/dtk/env.sh
          cd python/sglang/kernels/aot && python setup_hip.py bdist_wheel
          echo "=== sgl-kernel wheel ==="
          ls -lh dist/*.whl

      - name: Build sglang python package
        env:
          SETUPTOOLS_SCM_PRETEND_VERSION: "0.5.15.post1"
        run: |
          source /opt/dtk/env.sh
          source $HOME/.cargo/env
          cd python
          export SETUPTOOLS_SCM_PRETEND_VERSION="${SETUPTOOLS_SCM_PRETEND_VERSION}"
          python -m build --wheel --no-isolation -Cfeatures=all_hip
          echo "=== sglang wheel ==="
          ls -lh dist/*.whl

      - name: Repair and stage HCU wheels
        run: |
          source /opt/dtk/env.sh
          DTK_PKG_URL=${RESOURCE_SERVER}/dtk-pkg/dtk26.04/DTK-26.04-rc4-centos8-x86_64.tar.gz
          DTK_VER=$(echo $DTK_PKG_URL | grep -oP 'dtk\K[0-9]+\.[0-9]+' | tr -d '.')
          mkdir -p output
          shopt -s nullglob
          wheels=(python/sglang/kernels/aot/dist/*.whl python/dist/*.whl)
          if (( ${#wheels[@]} == 0 )); then
            echo "No HCU wheels were built" >&2
            exit 1
          fi
          CIUpload REPAIR \
            --dtk_version "${DTK_VER}" \
            --torch_version "${TORCH_VERSION}" \
            --git_commit "${WHEEL_COMMIT_SHA}" \
            --outputdir output \
            -f "${wheels[@]}"
          echo "=== Repaired wheels ==="
          ls -lh output/

          REPO_NAME="${GITHUB_REPOSITORY#*/}"
          STAGING_ROOT="${HCU_WHEEL_STAGING_ROOT:-/ci_public/sglang-das/hcu-wheel-staging}"
          STAGING_DIR="${STAGING_ROOT}/${GITHUB_REPOSITORY_OWNER}_${REPO_NAME}/pr-nightly-${GITHUB_RUN_ID}/${WHEEL_COMMIT_SHA}"
          WHEEL_DIR="${STAGING_DIR}/wheels"

          echo "Saving HCU nightly wheels to ${WHEEL_DIR}"
          rm -rf "${WHEEL_DIR}"
          mkdir -p "${WHEEL_DIR}"
          cp -v output/*.whl "${WHEEL_DIR}/"

          export STAGING_DIR
          python3 - <<'PY_MANIFEST'
          from email.parser import Parser
          import json
          import os
          import pathlib
          import zipfile

          staging_dir = pathlib.Path(os.environ["STAGING_DIR"])
          wheel_dir = staging_dir / "wheels"
          wheel_paths = sorted(wheel_dir.glob("*.whl"))
          wheel_local_versions = {}
          for path in wheel_paths:
              with zipfile.ZipFile(path) as archive:
                  metadata_names = [
                      name for name in archive.namelist() if name.endswith(".dist-info/METADATA")
                  ]
                  if len(metadata_names) != 1:
                      raise RuntimeError(f"Expected one METADATA file in {path.name}")
                  metadata = Parser().parsestr(
                      archive.read(metadata_names[0]).decode("utf-8")
                  )
              version = metadata.get("Version", "")
              _, separator, local_version = version.partition("+")
              if not separator or not local_version:
                  raise RuntimeError(f"Missing local version in {path.name}: {version}")
              wheel_local_versions[path.name] = local_version
          unique_local_versions = set(wheel_local_versions.values())
          local_version = next(iter(unique_local_versions)) if len(unique_local_versions) == 1 else None
          wheels = [str(path.relative_to(staging_dir)) for path in wheel_paths]
          manifest = {
              "repository": os.environ.get("GITHUB_REPOSITORY", ""),
              "run_id": os.environ.get("GITHUB_RUN_ID", ""),
              "commit_sha": os.environ.get("WHEEL_COMMIT_SHA", ""),
              "torch_version": os.environ.get("TORCH_VERSION", ""),
              "local_version": local_version,
              "wheel_local_versions": wheel_local_versions,
              "source": "nightly-test-hcu",
              "wheels": wheels,
          }
          (staging_dir / "manifest.json").write_text(json.dumps(manifest, indent=2) + "\n")
          PY_MANIFEST

          touch "${STAGING_DIR}/READY"
          chown -R "${HCU_RUNNER_HOST_UID}:${HCU_RUNNER_HOST_GID}" "${STAGING_DIR}" || true
          echo "HCU nightly wheels are staged locally at ${STAGING_DIR}"

      - name: Normalize workspace ownership
        if: always()
        run: |
          chown -R "${HCU_RUNNER_HOST_UID}:${HCU_RUNNER_HOST_GID}" "${GITHUB_WORKSPACE}"

  wait-hcu-wheels:
    name: Wait HCU wheels
    needs:
      - validate-config
      - probe-hcu-wheels
      - build-hcu-wheels
    if: >
      always() &&
      needs.validate-config.result == 'success' &&
      needs.probe-hcu-wheels.result == 'success' &&
      (needs.build-hcu-wheels.result == 'success' || needs.build-hcu-wheels.result == 'skipped')
    runs-on: ${{ needs.validate-config.outputs.runner_label }}
    outputs:
      wheel_urls: ${{ steps.wait.outputs.wheel_urls }}
    env:
      HCU_WHEEL_STAGING_ROOT: /ci_public/sglang-das/hcu-wheel-staging
      HCU_WHEEL_STAGING_CONTAINER_ROOT: /hcu-wheel-staging
      HCU_WHEEL_TARGET_SHA: ${{ needs.probe-hcu-wheels.outputs.full_sha }}
      HCU_WHEEL_TARGET_SHORT_SHA: ${{ needs.probe-hcu-wheels.outputs.short_sha }}
      PROBE_WHEEL_AVAILABLE: ${{ needs.probe-hcu-wheels.outputs.wheel_available }}
      PROBE_WHEEL_URLS: ${{ needs.probe-hcu-wheels.outputs.wheel_urls }}
    steps:
      - name: Wait for current commit HCU wheels
        id: wait
        run: |
          set -euo pipefail
          if [[ "${PROBE_WHEEL_AVAILABLE}" == "true" ]]; then
            echo "Reusing existing staged HCU wheels for ${HCU_WHEEL_TARGET_SHA}: ${PROBE_WHEEL_URLS}"
            echo "wheel_urls=${PROBE_WHEEL_URLS}" >> "${GITHUB_OUTPUT}"
            exit 0
          fi

          echo "Waiting for newly staged HCU wheels for ${HCU_WHEEL_TARGET_SHA}"
          echo "Host staging root: ${HCU_WHEEL_STAGING_ROOT}"
          python3 - <<'PY_WAIT' | tee /tmp/hcu_nightly_wheel_paths.out
          import os
          import pathlib
          import sys
          import time

          root = pathlib.Path(os.environ.get("HCU_WHEEL_STAGING_ROOT", "/ci_public/sglang-das/hcu-wheel-staging"))
          container_root = pathlib.Path(os.environ.get("HCU_WHEEL_STAGING_CONTAINER_ROOT", "/hcu-wheel-staging"))
          target_sha = os.environ["HCU_WHEEL_TARGET_SHA"]
          short_sha = os.environ.get("HCU_WHEEL_TARGET_SHORT_SHA", target_sha[:12])
          repo = os.environ["GITHUB_REPOSITORY"]
          owner, repo_name = repo.split("/", 1)
          repo_key = f"{owner}_{repo_name}"
          timeout_s = int(os.environ.get("HCU_WHEEL_WAIT_TIMEOUT", "3600"))
          interval_s = int(os.environ.get("HCU_WHEEL_WAIT_INTERVAL", "60"))

          def candidate_dirs():
              base = root / repo_key
              return sorted(
                  [path for path in base.glob(f"pr-*/{target_sha}") if path.is_dir()],
                  key=lambda path: path.stat().st_mtime,
                  reverse=True,
              )

          def wheel_kind(path: pathlib.Path):
              name = path.name.replace("-", "_")
              if name.startswith(("sgl_kernel_", "sglang_kernel_")):
                  return "sgl-kernel"
              if name.startswith("sglang_"):
                  return "sglang"
              return None

          def to_container_path(path: pathlib.Path) -> str:
              return str(container_root / path.relative_to(root))

          deadline = time.time() + timeout_s
          while time.time() < deadline:
              candidates = candidate_dirs()
              if not candidates:
                  print(f"[wait-hcu-wheels] no staged dirs under {root / repo_key}/pr-*/{target_sha}", file=sys.stderr)
              for staging_dir in candidates:
                  ready = staging_dir / "READY"
                  wheel_dir = staging_dir / "wheels"
                  if not ready.exists():
                      print(f"[wait-hcu-wheels] {staging_dir} has no READY", file=sys.stderr)
                      continue
                  if not wheel_dir.is_dir():
                      print(f"[wait-hcu-wheels] {staging_dir} has no wheels directory", file=sys.stderr)
                      continue
                  found = {}
                  for wheel in sorted(wheel_dir.glob("*.whl")):
                      kind = wheel_kind(wheel)
                      if kind and kind not in found:
                          found[kind] = wheel
                  missing = [name for name in ("sglang", "sgl-kernel") if name not in found]
                  if not missing:
                      paths = [to_container_path(found[name]) for name in ("sglang", "sgl-kernel")]
                      print(f"[wait-hcu-wheels] found staged wheels in {staging_dir}: {' '.join(paths)}", file=sys.stderr)
                      print(" ".join(paths))
                      sys.exit(0)
                  print(f"[wait-hcu-wheels] {staging_dir} missing {missing}; found={list(found)}", file=sys.stderr)
              print(f"[wait-hcu-wheels] waiting for local staged wheels for {short_sha}; sleep {interval_s}s", file=sys.stderr)
              time.sleep(interval_s)

          print(f"Timed out waiting for locally staged HCU wheels for {target_sha}", file=sys.stderr)
          sys.exit(1)
          PY_WAIT

          wheel_urls="$(tail -n 1 /tmp/hcu_nightly_wheel_paths.out)"
          if [[ -z "${wheel_urls}" ]]; then
            echo "No local staged wheel paths were produced." >&2
            exit 1
          fi
          echo "wheel_urls=${wheel_urls}" >> "${GITHUB_OUTPUT}"
          echo "HCU_CI_INSTALL_WHEEL_URLS=${wheel_urls}"

  nightly-hcu:
    name: Nightly HCU (${{ matrix.name }})
    needs:
      - validate-config
      - probe-hcu-wheels
      - build-hcu-wheels
      - wait-hcu-wheels
    if: >
      always() &&
      needs.validate-config.result == 'success' &&
      needs.probe-hcu-wheels.result == 'success' &&
      (needs.build-hcu-wheels.result == 'success' || needs.build-hcu-wheels.result == 'skipped') &&
      needs.wait-hcu-wheels.result == 'success'
    runs-on: ${{ needs.validate-config.outputs.runner_label }}
    timeout-minutes: ${{ matrix.timeout_minutes }}
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        include:
          - name: core-functional
            group: nightly-hcu-core-functional
            suite: nightly-hcu-core-functional
            nightly: "true"
            part: 0
            parts: 1
            timeout_per_file: 3000
            timeout_minutes: 180
            include_files: ""
          - name: one-hcu
            group: nightly-hcu-1
            suite: nightly-hcu-1
            nightly: "true"
            part: 0
            parts: 1
            timeout_per_file: 1200
            timeout_minutes: 120
            include_files: ""
          - name: accuracy-text-0
            group: nightly-hcu-accuracy-text
            suite: nightly-hcu-accuracy-text
            network_mode: bridge
            nightly: "true"
            part: 0
            parts: 2
            timeout_per_file: 4200
            timeout_minutes: 180
            include_files: ""
            extra_model_paths: "/public4/share:/public4/opendas/DL_DATA/llm-models:/public/opendas/DL_DATA/opencompass_data"
          - name: accuracy-text-1
            group: nightly-hcu-accuracy-text
            suite: nightly-hcu-accuracy-text
            network_mode: bridge
            nightly: "true"
            part: 1
            parts: 2
            timeout_per_file: 4200
            timeout_minutes: 180
            include_files: ""
            extra_model_paths: "/public4/share:/public4/opendas/DL_DATA/llm-models:/public/opendas/DL_DATA/opencompass_data"
          - name: reasoning-code-qwen3-32b
            group: nightly-hcu-accuracy-reasoning-code
            suite: nightly-hcu-accuracy-reasoning-code
            nightly: "true"
            part: 0
            parts: 1
            timeout_per_file: 14400
            timeout_minutes: 300
            include_files: registered/hcu/accuracy/bw1100/test_reasoning_code_qwen3_32b_hcu.py
            reasoning_model_key: qwen3_32b
            visible_devices: "0,1,2,3,4,5,6,7"
            extra_model_paths: "/public4/share:/public4/opendas/DL_DATA/llm-models:/public/opendas/DL_DATA/opencompass_data"
          - name: reasoning-code-qwen3-30b-a3b
            group: nightly-hcu-accuracy-reasoning-code
            suite: nightly-hcu-accuracy-reasoning-code
            nightly: "true"
            part: 0
            parts: 1
            timeout_per_file: 14400
            timeout_minutes: 300
            include_files: registered/hcu/accuracy/bw1100/test_reasoning_code_qwen3_30b_a3b_hcu.py
            reasoning_model_key: qwen3_30b_a3b
            visible_devices: "0,1,2,3,4,5,6,7"
            extra_model_paths: "/public4/share:/public4/opendas/DL_DATA/llm-models:/public/opendas/DL_DATA/opencompass_data"
          - name: reasoning-code-qwen36-35b-a3b
            group: nightly-hcu-accuracy-reasoning-code
            suite: nightly-hcu-accuracy-reasoning-code
            nightly: "true"
            part: 0
            parts: 1
            timeout_per_file: 14400
            timeout_minutes: 300
            include_files: registered/hcu/accuracy/bw1100/test_reasoning_code_qwen36_35b_a3b_hcu.py
            reasoning_model_key: qwen36_35b_a3b
            visible_devices: "0,1"
            extra_model_paths: "/public4/share:/public4/opendas/DL_DATA/llm-models:/public/opendas/DL_DATA/opencompass_data"
          - name: reasoning-code-qwen35-397b
            group: nightly-hcu-accuracy-reasoning-code
            suite: nightly-hcu-accuracy-reasoning-code
            nightly: "true"
            part: 0
            parts: 1
            timeout_per_file: 14400
            timeout_minutes: 300
            include_files: registered/hcu/accuracy/bw1100/test_reasoning_code_qwen35_397b_hcu.py
            reasoning_model_key: qwen35_397b
            visible_devices: "0,1,2,3,4,5,6,7"
            extra_model_paths: "/public4/share:/public4/opendas/DL_DATA/llm-models:/public/opendas/DL_DATA/opencompass_data"
          - name: vlm
            group: nightly-hcu-vlm
            suite: nightly-hcu-vlm
            nightly: "true"
            part: 0
            parts: 1
            timeout_per_file: 7800
            timeout_minutes: 300
            include_files: ""
            visible_devices: "0,1,2,3,4,5,6,7"
            extra_model_paths: "/public4/share:/public4/opendas/DL_DATA/llm-models"
          - name: perf-text-0
            group: nightly-hcu-perf-text
            suite: nightly-hcu-perf-text
            nightly: "true"
            part: 0
            parts: 5
            timeout_per_file: 7200
            timeout_minutes: 300
            include_files: ""
            visible_devices: "0,1,2,3,4,5,6,7"
            extra_model_paths: "/public4/share:/public4/opendas/DL_DATA/llm-models"
          - name: perf-text-1
            group: nightly-hcu-perf-text
            suite: nightly-hcu-perf-text
            nightly: "true"
            part: 1
            parts: 5
            timeout_per_file: 7200
            timeout_minutes: 300
            include_files: ""
            visible_devices: "0,1,2,3,4,5,6,7"
            extra_model_paths: "/public4/share:/public4/opendas/DL_DATA/llm-models"
          - name: perf-text-2
            group: nightly-hcu-perf-text
            suite: nightly-hcu-perf-text
            nightly: "true"
            part: 2
            parts: 5
            timeout_per_file: 7200
            timeout_minutes: 300
            include_files: ""
            visible_devices: "0,1,2,3,4,5,6,7"
            extra_model_paths: "/public4/share:/public4/opendas/DL_DATA/llm-models"
          - name: perf-text-3
            group: nightly-hcu-perf-text
            suite: nightly-hcu-perf-text
            nightly: "true"
            part: 3
            parts: 5
            timeout_per_file: 7200
            timeout_minutes: 300
            include_files: ""
            visible_devices: "0,1,2,3,4,5,6,7"
            extra_model_paths: "/public4/share:/public4/opendas/DL_DATA/llm-models"
          - name: perf-text-4
            group: nightly-hcu-perf-text
            suite: nightly-hcu-perf-text
            nightly: "true"
            part: 4
            parts: 5
            timeout_per_file: 7200
            timeout_minutes: 300
            include_files: ""
            visible_devices: "0,1,2,3,4,5,6,7"
            extra_model_paths: "/public4/share:/public4/opendas/DL_DATA/llm-models"
          - name: large-model-4-hcu
            group: nightly-hcu-large-model-4
            suite: nightly-hcu-large-model-4
            nightly: "true"
            part: 0
            parts: 1
            timeout_per_file: 5400
            timeout_minutes: 300
            include_files: ""
            visible_devices: "4,5,6,7"
            extra_model_paths: "/public4/share:/public4/opendas/DL_DATA/llm-models"
          - name: large-model-8-hcu-0
            group: nightly-hcu-large-model-8
            suite: nightly-hcu-large-model-8
            nightly: "true"
            part: 0
            parts: 4
            timeout_per_file: 14400
            timeout_minutes: 420
            include_files: ""
            visible_devices: "0,1,2,3,4,5,6,7"
            extra_model_paths: "/public4/share:/public4/opendas/DL_DATA/llm-models"
          - name: large-model-8-hcu-1
            group: nightly-hcu-large-model-8
            suite: nightly-hcu-large-model-8
            nightly: "true"
            part: 1
            parts: 4
            timeout_per_file: 14400
            timeout_minutes: 420
            include_files: ""
            visible_devices: "0,1,2,3,4,5,6,7"
            extra_model_paths: "/public4/share:/public4/opendas/DL_DATA/llm-models"
          - name: large-model-8-hcu-2
            group: nightly-hcu-large-model-8
            suite: nightly-hcu-large-model-8
            nightly: "true"
            part: 2
            parts: 4
            timeout_per_file: 14400
            timeout_minutes: 420
            include_files: ""
            visible_devices: "0,1,2,3,4,5,6,7"
            extra_model_paths: "/public4/share:/public4/opendas/DL_DATA/llm-models"
          - name: large-model-8-hcu-3
            group: nightly-hcu-large-model-8
            suite: nightly-hcu-large-model-8
            nightly: "true"
            part: 3
            parts: 4
            timeout_per_file: 14400
            timeout_minutes: 420
            include_files: ""
            visible_devices: "0,1,2,3,4,5,6,7"
            extra_model_paths: "/public4/share:/public4/opendas/DL_DATA/llm-models"
          - name: quant-opt-0
            group: nightly-hcu-quant-opt
            suite: nightly-hcu-quant-opt
            nightly: "true"
            part: 0
            parts: 4
            timeout_per_file: 15000
            timeout_minutes: 480
            include_files: ""
            visible_devices: "0,1,2,3,4,5,6,7"
            extra_model_paths: "/public4/share:/public4/opendas/DL_DATA/llm-models"
          - name: quant-opt-1
            group: nightly-hcu-quant-opt
            suite: nightly-hcu-quant-opt
            nightly: "true"
            part: 1
            parts: 4
            timeout_per_file: 15000
            timeout_minutes: 480
            include_files: ""
            visible_devices: "0,1,2,3,4,5,6,7"
            extra_model_paths: "/public4/share:/public4/opendas/DL_DATA/llm-models"
          - name: quant-opt-2
            group: nightly-hcu-quant-opt
            suite: nightly-hcu-quant-opt
            nightly: "true"
            part: 2
            parts: 4
            timeout_per_file: 15000
            timeout_minutes: 480
            include_files: ""
            visible_devices: "0,1,2,3,4,5,6,7"
            extra_model_paths: "/public4/share:/public4/opendas/DL_DATA/llm-models"
          - name: quant-opt-3
            group: nightly-hcu-quant-opt
            suite: nightly-hcu-quant-opt
            nightly: "true"
            part: 3
            parts: 4
            timeout_per_file: 15000
            timeout_minutes: 480
            include_files: ""
            visible_devices: "0,1,2,3,4,5,6,7"
            extra_model_paths: "/public4/share:/public4/opendas/DL_DATA/llm-models"
          - name: long-scheduler
            group: nightly-hcu-functional-long
            suite: nightly-hcu-functional-long
            nightly: "true"
            part: 0
            parts: 1
            timeout_per_file: 1800
            timeout_minutes: 120
            include_files: >-
              registered/scheduler/test_abort.py
              registered/scheduler/test_priority_scheduling.py
              registered/scheduler/test_no_chunked_prefill.py
              registered/tokenizer/test_skip_tokenizer_init.py
          - name: long-decoding
            group: nightly-hcu-functional-long
            suite: nightly-hcu-functional-long
            nightly: "true"
            part: 0
            parts: 1
            timeout_per_file: 1800
            timeout_minutes: 90
            include_files: >-
              registered/constrained_decoding/test_constrained_decoding.py
              registered/openai_server/features/test_json_mode.py
          - name: two-hcu
            group: nightly-hcu-2
            suite: nightly-hcu-2
            nightly: "true"
            part: 0
            parts: 1
            timeout_per_file: 1800
            timeout_minutes: 120
            include_files: ""
            visible_devices: "0,1"
          - name: four-hcu-functional
            group: nightly-hcu-4-functional
            suite: nightly-hcu-4-functional
            nightly: "true"
            part: 0
            parts: 1
            timeout_per_file: 2400
            timeout_minutes: 120
            include_files: ""
            visible_devices: "0,1,2,3"
          - name: api-models
            group: nightly-hcu-api-models
            suite: nightly-hcu-api-models
            nightly: "true"
            part: 0
            parts: 1
            timeout_per_file: 1800
            timeout_minutes: 180
            include_files: ""
    env:
      HCU_CI_IMAGE: ${{ needs.validate-config.outputs.image }}
      HCU_CI_CONTAINER_NAME: ci_sglang_hcu_nightly_${{ github.run_id }}_${{ matrix.name }}
      HCU_CI_SKIP_PULL: ${{ vars.HCU_CI_SKIP_PULL || '' }}
      HCU_CI_SKIP_DEPENDENCY_INSTALL: ${{ vars.HCU_CI_SKIP_DEPENDENCY_INSTALL || '' }}
      HCU_CI_SKIP_REQUIREMENTS_INSTALL: ${{ vars.HCU_CI_SKIP_REQUIREMENTS_INSTALL || '' }}
      HCU_CI_SKIP_SGLANG_BUILD: ${{ vars.HCU_CI_SKIP_SGLANG_BUILD || '' }}
      HCU_CI_VISIBLE_DEVICES: ${{ matrix.visible_devices || vars.HCU_CI_VISIBLE_DEVICES || '' }}
      HCU_CI_NETWORK_MODE: ${{ matrix.network_mode || 'bridge' }}
      HCU_MODEL_EXTRA_HOST_PATHS: ${{ matrix.extra_model_paths || '' }}
      SGLANG_HCU_GSM8K_DATA_PATH: /public/opendas/DL_DATA/opencompass_data/gsm8k/test.jsonl
      SGLANG_HCU_MMLU_DATASET_PATH: /public/opendas/DL_DATA/llm-models/datasets/mmlu
      SGLANG_HCU_MATH500_DATA_PATH: /public/opendas/DL_DATA/opencompass_data/math/test_prm800k_500.jsonl
      SGLANG_HCU_HUMANEVAL_DATA_PATH: /public/opendas/DL_DATA/opencompass_data/humaneval/human-eval-v2-20210705.jsonl
      HCU_CI_INSTALL_WHEEL_URLS: ${{ needs.wait-hcu-wheels.outputs.wheel_urls || '' }}
      HCU_CI_USE_INSTALLED_WHEELS: "1"
      HCU_WHEEL_STAGING_ROOT: /ci_public/sglang-das/hcu-wheel-staging
      HCU_WHEEL_STAGING_CONTAINER_ROOT: /hcu-wheel-staging
      HCU_ACCURACY_SHARED_ROOT: /ci_public/sglang-das/hcu-accuracy
      HCU_REASONING_CODE_SHARED_ROOT: /ci_public/sglang-das/hcu-reasoning-code
    steps:
      - name: Check job filter
        id: filter
        env:
          JOB_FILTER: ${{ needs.validate-config.outputs.job_filter }}
          MATRIX_GROUP: ${{ matrix.group }}
        run: |
          set -euo pipefail
          if [[ "${JOB_FILTER}" == "all" || "${JOB_FILTER}" == "${MATRIX_GROUP}" ]]; then
            echo "should_run=true" >> "${GITHUB_OUTPUT}"
            echo "Running group ${MATRIX_GROUP}"
          else
            echo "should_run=false" >> "${GITHUB_OUTPUT}"
            echo "Skipping group ${MATRIX_GROUP}; selected job_filter=${JOB_FILTER}"
          fi

      - name: Checkout code
        if: steps.filter.outputs.should_run == 'true'
        env:
          CHECKOUT_REF: ${{ needs.validate-config.outputs.target_ref }}
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          CHECKOUT_DIR="${RUNNER_TEMP}/hcu-sglang-${GITHUB_RUN_ID}-nightly-${{ matrix.name }}"
          rm -rf "${CHECKOUT_DIR}"
          mkdir -p "${CHECKOUT_DIR}"
          echo "HCU_CI_CHECKOUT_DIR=${CHECKOUT_DIR}" >> "${GITHUB_ENV}"
          git init "${CHECKOUT_DIR}"
          cd "${CHECKOUT_DIR}"
          git remote add origin "https://github.com/${GITHUB_REPOSITORY}.git"
          auth="$(printf 'x-access-token:%s' "${GH_TOKEN}" | base64 | tr -d '\n')"
          git \
            -c http.https://github.com/.extraheader="AUTHORIZATION: basic ${auth}" \
            -c http.https://github.com.proxy= \
            -c http.proxy= \
            -c https.proxy= \
            fetch --no-tags --depth=1 origin "${CHECKOUT_REF}"
          git checkout --force FETCH_HEAD

      - name: Setup HCU container
        if: steps.filter.outputs.should_run == 'true'
        run: |
          GITHUB_WORKSPACE="${HCU_CI_CHECKOUT_DIR}" \
            bash "${HCU_CI_CHECKOUT_DIR}/scripts/ci/hcu/hcu_ci_start_container.sh" \
              --image "${HCU_CI_IMAGE}" \
              --container-name "${HCU_CI_CONTAINER_NAME}"

      - name: Install dependencies
        if: steps.filter.outputs.should_run == 'true'
        run: bash "${HCU_CI_CHECKOUT_DIR}/scripts/ci/hcu/hcu_ci_install_dependency.sh"

      - name: Install reasoning/code evaluation dependencies
        if: >-
          steps.filter.outputs.should_run == 'true' &&
          matrix.group == 'nightly-hcu-accuracy-reasoning-code'
        run: >-
          bash "${HCU_CI_CHECKOUT_DIR}/scripts/ci/hcu/hcu_ci_install_reasoning_code_deps.sh"

      - name: Run nightly HCU suite
        id: run_suite
        if: steps.filter.outputs.should_run == 'true'
        env:
          INCLUDE_FILES: ${{ matrix.include_files }}
          CONTINUE_ON_ERROR_FLAG: ${{ needs.validate-config.outputs.continue_on_error_flag }}
        run: |
          set -euo pipefail

          cmd=(
            python3 run_suite.py
            --hw hcu
            --suite "${{ matrix.suite }}"
            --auto-partition-id "${{ matrix.part }}"
            --auto-partition-size "${{ matrix.parts }}"
            --timeout-per-file "${{ matrix.timeout_per_file }}"
          )

          if [[ "${{ matrix.nightly }}" == "true" ]]; then
            cmd+=(--nightly)
          fi

          if [[ -n "${CONTINUE_ON_ERROR_FLAG}" ]]; then
            cmd+=("${CONTINUE_ON_ERROR_FLAG}")
          fi

          if [[ -n "${INCLUDE_FILES}" ]]; then
            for include_file in ${INCLUDE_FILES}; do
              cmd+=(--include-file "${include_file}")
            done
          fi

          echo "Running ${{ matrix.suite }} partition ${{ matrix.part }}/${{ matrix.parts }} nightly=${{ matrix.nightly }}"
          echo "HCU_CI_INSTALL_WHEEL_URLS=${HCU_CI_INSTALL_WHEEL_URLS}"
          exec_args=(
            -w /sglang-checkout/test
            --container-name "${HCU_CI_CONTAINER_NAME}"
          )
          if [[ "${{ matrix.group }}" == "nightly-hcu-accuracy-text" ]]; then
            exec_args+=(
              -e SGLANG_HCU_ACCURACY_RESULT_DIR=/sglang-checkout/test-results/hcu-accuracy
            )
          fi
          if [[ "${{ matrix.group }}" == "nightly-hcu-accuracy-reasoning-code" ]]; then
            log_dir="${HCU_CI_CHECKOUT_DIR}/test-results/hcu-reasoning-code/.logs"
            mkdir -p "${log_dir}"
            bash "${HCU_CI_CHECKOUT_DIR}/scripts/ci/hcu/hcu_ci_exec.sh" "${exec_args[@]}" -- \
              "${cmd[@]}" 2>&1 | tee "${log_dir}/${{ matrix.reasoning_model_key }}-suite.log"
          else
            bash "${HCU_CI_CHECKOUT_DIR}/scripts/ci/hcu/hcu_ci_exec.sh" "${exec_args[@]}" -- \
              "${cmd[@]}"
          fi

      - name: Judge HumanEval in a restricted container
        id: judge_humaneval
        if: >-
          always() &&
          steps.filter.outputs.should_run == 'true' &&
          matrix.group == 'nightly-hcu-accuracy-reasoning-code'
        run: |
          set -euo pipefail
          log_dir="${HCU_CI_CHECKOUT_DIR}/test-results/hcu-reasoning-code/.logs"
          mkdir -p "${log_dir}"
          bash "${HCU_CI_CHECKOUT_DIR}/scripts/ci/hcu/hcu_ci_judge_humaneval.sh" \
            "${{ matrix.reasoning_model_key }}" 2>&1 | \
            tee "${log_dir}/${{ matrix.reasoning_model_key }}-judge.log"

      - name: Capture HCU runtime image ID
        if: >-
          always() &&
          steps.filter.outputs.should_run == 'true' &&
          (matrix.group == 'nightly-hcu-accuracy-text' ||
           matrix.group == 'nightly-hcu-accuracy-reasoning-code')
        run: |
          set -euo pipefail
          image_id="$(docker inspect --format '{{.Image}}' "${HCU_CI_CONTAINER_NAME}" 2>/dev/null || true)"
          if [[ -z "${image_id}" ]]; then
            image_id="$(docker image inspect --format '{{.Id}}' "${HCU_CI_IMAGE}" 2>/dev/null || true)"
          fi
          echo "HCU_CI_RUNTIME_IMAGE_ID=${image_id:-unknown}" >> "${GITHUB_ENV}"
          echo "Resolved HCU runtime image ID: ${image_id:-unknown}"

      - name: Cleanup HCU container
        if: always() && steps.filter.outputs.should_run == 'true'
        run: bash "${HCU_CI_CHECKOUT_DIR}/scripts/ci/hcu/hcu_ci_cleanup_container.sh"

      - name: Publish reasoning/code results to shared storage
        if: >-
          always() &&
          steps.filter.outputs.should_run == 'true' &&
          matrix.group == 'nightly-hcu-accuracy-reasoning-code'
        env:
          SUITE_OUTCOME: ${{ steps.run_suite.outcome }}
          JUDGE_OUTCOME: ${{ steps.judge_humaneval.outcome }}
          HCU_REASONING_COMMIT_SHA: ${{ needs.probe-hcu-wheels.outputs.full_sha }}
          HCU_REASONING_TARGET_REF: ${{ needs.validate-config.outputs.target_ref }}
        run: |
          set -euo pipefail
          outcome="failure"
          if [[ "${SUITE_OUTCOME}" == "success" && "${JUDGE_OUTCOME}" == "success" ]]; then
            outcome="success"
          elif [[ "${SUITE_OUTCOME}" == "cancelled" || "${JUDGE_OUTCOME}" == "cancelled" ]]; then
            outcome="cancelled"
          fi
          result_root="${HCU_CI_CHECKOUT_DIR}/test-results/hcu-reasoning-code"
          python_bin="$(command -v python3.12 || command -v python3.10 || command -v python3.9 || command -v python3.8 || command -v python3)"
          "${python_bin}" "${HCU_CI_CHECKOUT_DIR}/scripts/ci/hcu/hcu_ci_publish_reasoning_code.py" \
            --local-model-dir "${result_root}/${{ matrix.reasoning_model_key }}" \
            --log-file "${result_root}/.logs/${{ matrix.reasoning_model_key }}-suite.log" \
            --log-file "${result_root}/.logs/${{ matrix.reasoning_model_key }}-judge.log" \
            --shared-root "${HCU_REASONING_CODE_SHARED_ROOT}" \
            --run-id "${GITHUB_RUN_ID}" \
            --run-attempt "${GITHUB_RUN_ATTEMPT}" \
            --model-key "${{ matrix.reasoning_model_key }}" \
            --outcome "${outcome}" \
            --target-ref "${HCU_REASONING_TARGET_REF}" \
            --commit-sha "${HCU_REASONING_COMMIT_SHA}" \
            --image-ref "${HCU_CI_IMAGE}" \
            --image-id "${HCU_CI_RUNTIME_IMAGE_ID:-unknown}" \
            --runner-name "${RUNNER_NAME}" \
            --retention-days 14

      - name: Publish HCU accuracy results to shared storage
        if: >-
          always() &&
          steps.filter.outputs.should_run == 'true' &&
          matrix.group == 'nightly-hcu-accuracy-text'
        env:
          PARTITION_OUTCOME: ${{ steps.run_suite.outcome }}
          HCU_ACCURACY_COMMIT_SHA: ${{ needs.probe-hcu-wheels.outputs.full_sha }}
          HCU_ACCURACY_TARGET_REF: ${{ needs.validate-config.outputs.target_ref }}
        run: |
          set -euo pipefail
          result_dir="${RUNNER_TEMP}/hcu-sglang-${GITHUB_RUN_ID}-nightly-${{ matrix.name }}/test-results/hcu-accuracy"
          python_bin="$(command -v python3.12 || command -v python3.10 || command -v python3.9 || command -v python3.8 || command -v python3)"
          "${python_bin}" "${HCU_CI_CHECKOUT_DIR}/scripts/ci/hcu/hcu_ci_publish_accuracy.py" \
            --local-results-dir "${result_dir}" \
            --shared-root "${HCU_ACCURACY_SHARED_ROOT}" \
            --run-id "${GITHUB_RUN_ID}" \
            --run-attempt "${GITHUB_RUN_ATTEMPT}" \
            --partition "${{ matrix.name }}" \
            --outcome "${PARTITION_OUTCOME}" \
            --target-ref "${HCU_ACCURACY_TARGET_REF}" \
            --commit-sha "${HCU_ACCURACY_COMMIT_SHA}" \
            --image-ref "${HCU_CI_IMAGE}" \
            --image-id "${HCU_CI_RUNTIME_IMAGE_ID:-unknown}" \
            --runner-name "${RUNNER_NAME}" \
            --retention-days 14

  nightly-hcu-finish:
    name: Nightly HCU finish
    needs:
      - validate-config
      - probe-hcu-wheels
      - build-hcu-wheels
      - wait-hcu-wheels
      - nightly-hcu
    if: always()
    runs-on: ${{ needs.validate-config.outputs.runner_label || vars.HCU_CI_RUNNER_LABEL }}
    steps:
      - name: Check all dependent job statuses
        run: |
          set -euo pipefail
          json_needs='${{ toJson(needs) }}'
          job_names=$(echo "${json_needs}" | jq -r 'keys_unsorted[]')

          for job in ${job_names}; do
            result=$(echo "${json_needs}" | jq -r --arg j "${job}" '.[$j].result')
            echo "${job}: ${result}"

            if [[ "${job}" == "build-hcu-wheels" && "${result}" == "skipped" ]]; then
              continue
            fi

            if [[ "${result}" == "failure" || "${result}" == "cancelled" ]]; then
              echo "The above jobs failed."
              exit 1
            fi
          done

          echo "All HCU nightly jobs completed successfully or were intentionally skipped."

  notify-hcu-accuracy-feishu:
    name: Notify HCU accuracy to Feishu
    needs:
      - validate-config
      - probe-hcu-wheels
      - nightly-hcu
      - nightly-hcu-finish
    if: >-
      always() &&
      github.ref_name == github.event.repository.default_branch &&
      needs.validate-config.result == 'success' &&
      (needs.validate-config.outputs.job_filter == 'all' ||
       needs.validate-config.outputs.job_filter == 'nightly-hcu-accuracy-text')
    runs-on: ${{ needs.validate-config.outputs.runner_label || vars.HCU_CI_RUNNER_LABEL }}
    environment: hcu-ci-notify
    steps:
      - name: Checkout notification source
        env:
          CHECKOUT_REF: ${{ github.sha }}
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          checkout_dir="${RUNNER_TEMP}/hcu-notify-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          rm -rf "${checkout_dir}"
          mkdir -p "${checkout_dir}"
          echo "HCU_NOTIFY_SOURCE_DIR=${checkout_dir}" >> "${GITHUB_ENV}"
          git init "${checkout_dir}"
          cd "${checkout_dir}"
          git remote add origin "https://github.com/${GITHUB_REPOSITORY}.git"
          auth="$(printf 'x-access-token:%s' "${GH_TOKEN}" | base64 | tr -d '\n')"
          git \
            -c http.https://github.com/.extraheader="AUTHORIZATION: basic ${auth}" \
            -c http.https://github.com.proxy= \
            -c http.proxy= \
            -c https.proxy= \
            fetch --no-tags --depth=1 origin "${CHECKOUT_REF}"
          git checkout --force FETCH_HEAD

      - name: Send signed Feishu accuracy card
        env:
          FEISHU_HCU_CI_WEBHOOK: ${{ secrets.FEISHU_HCU_CI_WEBHOOK }}
          FEISHU_HCU_CI_SIGNING_SECRET: ${{ secrets.FEISHU_HCU_CI_SIGNING_SECRET }}
          HCU_ACCURACY_RESULTS_DIR: /ci_public/sglang-das/hcu-accuracy/run-${{ github.run_id }}/attempt-${{ github.run_attempt }}
          HCU_ACCURACY_BRANCH: ${{ github.ref_name }}
          HCU_ACCURACY_TARGET_REF: ${{ needs.validate-config.outputs.target_ref }}
          HCU_ACCURACY_COMMIT_SHA: ${{ needs.probe-hcu-wheels.outputs.full_sha }}
          HCU_ACCURACY_IMAGE: ${{ needs.validate-config.outputs.image }}
          HCU_ACCURACY_WORKFLOW_RESULT: ${{ needs.nightly-hcu.result }}
          HCU_ACCURACY_RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          set -euo pipefail
          python_bin="$(command -v python3.12 || command -v python3.10 || command -v python3.9 || command -v python3.8 || command -v python3)"
          "${python_bin}" "${HCU_NOTIFY_SOURCE_DIR}/scripts/ci/hcu/hcu_ci_notify_feishu.py" \
            --results-dir "${HCU_ACCURACY_RESULTS_DIR}" \
            --branch "${HCU_ACCURACY_BRANCH}" \
            --target-ref "${HCU_ACCURACY_TARGET_REF}" \
            --commit-sha "${HCU_ACCURACY_COMMIT_SHA}" \
            --image "${HCU_ACCURACY_IMAGE}" \
            --workflow-result "${HCU_ACCURACY_WORKFLOW_RESULT}" \
            --run-url "${HCU_ACCURACY_RUN_URL}" \
            --run-id "${GITHUB_RUN_ID}" \
            --run-attempt "${GITHUB_RUN_ATTEMPT}"
